# CANUTO — dependencias del stack COMPLETO (inferencia + UI + API + entrenamiento).
# Python 3.11+ (verificado también en 3.12).
#
# NOTA: para SOLO extraer PDF -> Markdown y armar el dataset NO hace falta este
# archivo (ni torch); usar el venv ligero de requirements-extract.txt (o install.sh).
#
# torch va aparte (el wheel depende del hardware); instalar ANTES:
#   CPU:  pip install torch --index-url https://download.pytorch.org/whl/cpu
#   GPU (workstation, RTX 5080):
#         pip install torch --index-url https://download.pytorch.org/whl/cu130
#   La RTX 5080 es Blackwell y necesita CUDA 13 (cu130); el driver CUDA 13.2 la corre.
# luego: pip install -r requirements.txt   (o usar install.sh)
#
# El OCR de escaneados usa docling en un venv APARTE (requirements-docling.txt),
# porque exige transformers moderno que rompe el stack de inferencia/entrenamiento.

# --- Extracción PDF -> Markdown ---
pymupdf4llm==1.28.0
pdfplumber==0.11.9
PyMuPDF==1.28.0

# --- Configuración ---
PyYAML==6.0.3
python-dotenv==1.2.2

# --- Modelo de lenguaje (HuggingFace) ---
transformers==4.46.3
tokenizers==0.20.3
safetensors==0.7.0
accelerate==1.13.0
huggingface_hub==0.36.2
numpy==2.4.6

# --- Fine-tuning con LoRA / QLoRA ---
peft==0.19.1
trl==0.12.2
datasets==4.8.5
bitsandbytes   # cuantizacion 4-bit para entrenar 7B/14B en la GPU de 16 GB

# --- Metricas de evaluacion (scripts/evaluate.py) ---
bert-score==0.3.13
rouge-score==0.1.2
sacrebleu==2.4.3

# --- Interfaz web Django (CANUTO) ---
Django==5.2.15

# --- Interfaz web alternativa (Gradio, opcional) ---
gradio==6.15.2

# --- API REST (integración con SIRIUS) ---
fastapi==0.136.3
starlette==1.2.0
uvicorn[standard]==0.48.0
pydantic==2.13.4
