# CANUTO — dependencias del stack COMPLETO (inferencia + UI + API + entrenamiento).
# Python 3.11+ (verificado también en 3.12).
#
# NOTA: para SOLO extraer PDF -> Markdown y armar el dataset NO hace falta este
# archivo (ni torch); usar el venv ligero de requirements-extract.txt (o install.sh).
#
# torch va aparte (el wheel depende del hardware); instalar ANTES:
#   CPU:  pip install torch==2.12.0 --index-url https://download.pytorch.org/whl/cpu
#   CUDA: pip install torch==2.12.0 --index-url https://download.pytorch.org/whl/cu126
#         (cu126 = única línea con torch 2.12 para cp312; corre bajo CUDA 12.6+/13.x)
# luego: pip install -r requirements.txt
#
# El OCR de escaneados usa docling en un venv APARTE (requirements-docling.txt),
# porque exige transformers moderno que rompe el stack de inferencia/entrenamiento.

# --- Extracción PDF -> Markdown ---
pymupdf4llm==1.28.0
pdfplumber==0.11.9
PyMuPDF==1.28.0

# --- Configuración ---
PyYAML==6.0.3
python-dotenv==1.2.2

# --- Modelo de lenguaje (HuggingFace) ---
transformers==4.46.3
tokenizers==0.20.3
safetensors==0.7.0
accelerate==1.13.0
huggingface_hub==0.36.2
numpy==2.4.6

# --- Fine-tuning con LoRA (solo necesario para scripts/train.py) ---
peft==0.19.1
trl==0.12.2
datasets==4.8.5

# --- Interfaz web Django (CANUTO) ---
Django==5.2.15

# --- Interfaz web alternativa (Gradio, opcional) ---
gradio==6.15.2

# --- API REST (integración con SIRIUS) ---
fastapi==0.136.3
starlette==1.2.0
uvicorn[standard]==0.48.0
pydantic==2.13.4
