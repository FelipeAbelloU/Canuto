# Dependencias de CANUTO (extraccion + dataset + entrenamiento + inferencia + web).
# Workstation Ubuntu, Python 3.12.
#
# torch va aparte y ANTES: la RTX 5080 es Blackwell y necesita CUDA 13 (cu130):
#   pip install torch --index-url https://download.pytorch.org/whl/cu130
# luego: pip install -r requirements.txt   (install.sh hace los dos pasos)
#
# El OCR con docling va en un venv aparte (requirements-docling.txt).

# --- Extraccion PDF -> Markdown ---
pymupdf4llm==1.28.0
pdfplumber==0.11.9
PyMuPDF==1.28.0

# --- Configuracion ---
PyYAML==6.0.3

# --- Modelo de lenguaje ---
transformers==4.46.3
tokenizers==0.20.3
safetensors==0.7.0
accelerate==1.13.0
huggingface_hub==0.36.2
numpy==2.4.6

# --- Entrenamiento con QLoRA ---
peft==0.19.1
trl==0.12.2
datasets==4.8.5
bitsandbytes   # cuantizacion 4-bit para que el 7B quepa en la GPU de 16 GB

# --- Interfaz web ---
Django==5.2.15
