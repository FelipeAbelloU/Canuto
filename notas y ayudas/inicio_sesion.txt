# Paso 0 — venv aparte CON GPU (workstation Linux, Python 3.12, CUDA 13.2 -> cu126)
#   Atajo: bash install-docling.sh  (crea el venv, instala y verifica la GPU)
python3 -m venv venv-docling
source venv-docling/bin/activate
pip install torch --index-url https://download.pytorch.org/whl/cu126   # CUDA primero
pip install -r requirements-docling.txt --extra-index-url https://download.pytorch.org/whl/cu126

# Paso 1 — smoke test docling en 5–10 escaneados variados y REVISAR calidad
python scripts/extract_text.py --ocr --years 2015 --limit 10

# Paso 2 — si la calidad es buena, corrida completa del OCR
python scripts/extract_text.py --ocr